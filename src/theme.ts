// Shared design tokens for the lavender dashboard theme.

// Base card surface — white with a lavender hairline border and a soft
// lavender-tinted shadow. Padding is applied at each call site.
export const softCard =
  "rounded-2xl border border-lavender-200/70 bg-white shadow-soft";

// Small uppercase section label.
export const eyebrow =
  "text-[10px] font-bold uppercase tracking-[0.14em] text-lavender-500";
