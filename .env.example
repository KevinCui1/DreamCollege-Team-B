# Copy this file to .env.local and fill in your key, then restart `npm run dev`.
# This key is used ONLY by the dev/preview server middleware (see vite.config.ts
# and vite-plugins/bestNextTaskApi.ts). It is never bundled into client code.
#
#   cp .env.example .env.local
#
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
